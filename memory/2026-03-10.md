# 2026-03-10

- Finalizei deploy de produção do projeto `controlla-fiscal` (Next.js) como serviço `systemd` (`controlla-fiscal.service`) atrás de nginx em `/controlla`, com app servindo em `127.0.0.1:3020`.
- Migrei persistência para PostgreSQL (`controlla_fiscal`), ajustei `prisma/schema.prisma` para provider `postgresql`, apliquei migrations e seed.
- Corrigi bugs críticos de UX/fluxo: ações de editar/excluir/importar quebrando por `basePath`; normalizei rotas para `/controlla/...` e paginação de clientes.
- Tornei gráficos do dashboard dinâmicos (dados reais do banco) no lugar de placeholders estáticos.
- Implementei exclusão em lote de clientes + botão selecionar tudo, além de reset do sistema com confirmação dupla (depois movido para Configurações).
- Reestruturei Configurações com abas (perfil, escritório, notificações, segurança, acesso/permissões), incluí campo de backup e botão sair.
- Configurei backup horário via cron (`/etc/cron.d/controlla-fiscal-backup`) usando script `/usr/local/bin/controlla-hourly-backup.sh`; destino configurável por `.backup-path`, fallback em `/root/Downloads`.
- Corrigi erro de aplicação no reset (digest `3058322077`) ao trocar `throw` por redirect com mensagem amigável.
- Implementei busca global real no header (`/controlla/search`) com normalização de acentos e filtros por clientes/declarações/guias.
- Substituí relatórios crus por exportação Excel profissional (`exceljs`): tema visual, filtros, cabeçalho congelado, fórmulas/somatórios e abas executivas.
- Melhorei feedback UX: toasts globais por query, mensagens de sucesso/erro, botões com loading, validações obrigatórias, validação de CNPJ/CPF e importação com progresso.
- Melhorei responsividade mobile: menu visível no celular (dropdown), ajuste de distribuição no header (usuário+sair acima, busca abaixo, menu abaixo).
- Iniciei camada de segurança operacional: RBAC server-side (`requirePermission`/`hasPermission`) e soft delete com lixeira (`deletedAt`) para clientes/declarações/guias, com tela `/trash` e ação de restaurar.
