
## 2026-03-04 20:12 UTC - OTServer/MyAAC intervention log (durable)

- Confirmed site stack/location: MyAAC in `/var/www/myaac` with DB `otserv`.
- News pipeline work:
  - Inserted `myaac_news` entries for SUMMER/WINTER updates.
  - Fixed visibility by setting valid `type/category` for MyAAC listing.
  - Added and centered image for SUMMER UPDATE 2025 at `/var/www/myaac/images/news/summer-update-2025.jpg`.
- Responsive patch applied in template CSS:
  - File: `/var/www/myaac/templates/tibiacom/responsive-mobile.css`.
- Payment/redirect hardening:
  - PoloPag scripts in OT were disabled/renamed (`pix_polopag.lua`, `pix_class.lua`, `polopag.py`) after user confirmed Mercado Pago is the real gateway.
  - MyAAC login redirect hardening added to block external open redirects (prevent off-domain jumps like lattorre).
  - Cleared MyAAC php sessions and twig cache after redirect hardening.
- Root cause of in-game "Get Tibia Coins" wrong URL found in client package script:
  - `modules/game_store/game_store.lua` had `WEBSITE_GETCOINS = "https://lattorre.com/?donatepaypal"`.
  - Clarified limitation: server cannot change already-installed clients; only downloadable package can be patched.
- Performance diagnostics summary:
  - Infra stable (CPU/memory/disk OK), DB tuning active (`innodb_buffer_pool_size=1G`, slow log ON, `long_query_time=0.5`).
  - Save cycle itself fast (~40 ms).
  - Main lag suspicion shifted to duplicate event/action registrations in OT scripts.
- Safe dedup pass attempts and rollback discipline:
  - Restored previous removals when user requested full 1:1 review.
  - Final dedup pass executed with backup:
    - Backup folder: `/opt/otserv/.backup-dedup-final-2026-03-04-201115`.
    - Additional backup from earlier pass: `/opt/otserv/.backup-lagfix-2026-03-04-200449`.
  - `otserv.service` restarted multiple times successfully after edits.
- Heartbeat reminder operational issue noted:
  - `/root/.openclaw/workspace/scripts/check-emails.sh` repeatedly timed out/SIGTERM around 19:00 UTC checks; needs script-level fix.
